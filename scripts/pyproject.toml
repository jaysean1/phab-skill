[project]
name = "tickets-skill"
version = "0.1.0"
description = "Standalone Phabricator ticket management for Claude Code skills"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.25.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0",
]

[tool.uv]
# Mark as non-package (scripts only, no build)
package = false
